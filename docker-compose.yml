version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./agentlens.db
      - SESSIONS_DIR=./sessions
    volumes:
      - ./backend/sessions:/app/sessions
